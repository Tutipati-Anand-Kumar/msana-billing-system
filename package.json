{
  "name": "msanabilling-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && node scripts/copy-build.js && cd server && npm install"
  }
}